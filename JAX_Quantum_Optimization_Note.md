# JAX 量子线路模拟优化笔记

## 1. 核心挑战：JAX 中的深层量子线路模拟

在使用 JAX 模拟用于混合量子-经典机器学习的参数化量子线路 (PQC) 时，我们面临着**动态量子演化**与**静态 JIT 编译**之间的根本冲突。

### 1.1 "循环展开 (Loop Unrolling)" 爆炸
在标准实现中，通过深度为 $D$ 的线路传播可观测量的过程通常是按层顺序执行的：
```python
# 标准循环 (Python)
for layer in layers:
    state = apply_layer(state, layer)
```
当这段代码被 `@jax.jit` 装饰时，JAX 会试图完全**展开**这个循环。
- **计算图大小**: $O(D)$
- **编译时间**: $O(D)$
- **内存占用**: $O(D)$

对于深层线路（例如 $D=100$ 或 $D=1000$），编译时间会变得不可接受（数分钟甚至数小时），且庞大的计算图极易导致内存溢出。

### 1.2 动态结构问题
量子算符传播（海森堡绘景）天然会产生数量可变的 Pauli 项。
- 第 1 层：$Z_1 \to Z_1 \cos\theta + X_1 \sin\theta$ (2 项)
- 第 2 层：项数相乘并分支... (指数级增长)
- **JAX 的限制**: JAX JIT 要求**静态形状 (Static Shapes)**。你不能在循环迭代之间传递长度可变的 Pauli 字符串列表。这直接阻碍了 `jax.lax.scan` 的使用（因为 `scan` 要求 carry 具有固定大小）。

---

## 2. 解决方案：基于 `jax.lax.scan` 的固定基底投影

为了实现与深度无关的 $O(1)$ 编译时间，我们必须使用 `jax.lax.scan`。这要求我们将动态的 Pauli 和转换成**固定大小的向量**。

### 2.1 策略：子空间近似 (Galerkin 投影)

我们不再追踪任意列表的 Pauli 字符串，而是定义一个我们关心的固定**基底 (Basis)**。例如，所有 Weight $\le 2$ 的 Pauli 字符串。

$$ \mathcal{B} = \{ I, X_1, \dots, Z_N, X_1 X_2, \dots, Z_{N-1} Z_N \} $$

基底大小 $|\mathcal{B}| \approx 1 + 3N + 9 \binom{N}{2} = O(N^2)$。对于 $N=10$，基底大小约为 436。

**状态表示**:
状态不再是一堆对象，而是一个系数构成的稠密向量 $\mathbf{c} \in \mathbb{R}^{|\mathcal{B}|}$。

### 2.2 算法流程

1.  **基底生成 (预计算)**:
    生成截断子空间（如 Weight $\le 2$）内所有 Pauli 字符串的标准列表。建立 `Signature -> Index` 的映射。

2.  **Scan 步进函数 ($O(1)$ 复杂度)**:
    定义函数 `step(coeff_vector, layer_params)`:
    *   **重建 (Reconstruct)**: 将 `coeff_vector` 转换回稀疏的 PauliSum 对象（仅需符号结构）。
    *   **传播 (Propagate)**: 使用 `pypoli` 演化**一层**线路。这会产生新的项，其中可能包含基底之外的项（Weight > 2）。
    *   **投影 (Project)**: 将结果项映射回 `coeff_vector`。
        *   如果项在 $\mathcal{B}$ 中，将其系数累加到向量对应位置。
        *   如果项不在 $\mathcal{B}$ 中，直接丢弃（截断）。
    *   **掩码 (Masking)**: 使用 `jnp.where` 或掩码对系数进行数值截断（将小系数置零），保持向量形状不变。

3.  **执行**:
    ```python
    final_coeffs, _ = jax.lax.scan(scan_step, init_coeffs, all_layer_params)
    ```

### 2.3 为什么这有效

*   **固定 I/O**: `step` 函数的输入和输出永远是大小为 $|\mathcal{B}|$ 的向量。
*   **一次编译**: JAX 只会编译 `step` 函数**一次**。深度 $D$ 的循环由 XLA 运行时处理，而不是通过展开计算图。
*   **隐式截断**: 投影步骤自然地强制执行了物理近似（Weight 截断），从而保持模拟的高效性。

---

## 3. 性能基准测试

**场景**: 10 Qubits, 25 Layers, 混合训练循环。

| 指标 | 标准循环 (Unrolled) | 固定基底 Scan | 提升幅度 |
| :--- | :--- | :--- | :--- |
| **编译时间** | > 2 分钟 (或超时) | **< 1 秒** | **~100倍 - $\infty$** |
| **单步时间** | N/A (未完成) | **~5.7 毫秒** | **实时** |
| **可扩展性** | 线性 $O(D)$ | 常数 $O(1)$ | 完美扩展 |

## 4. 关键总结

1.  **结构决定速度**: 要利用 JAX 处理离散/稀疏问题（如 Pauli 串），必须将其映射为静态、稠密的表示（向量/矩阵）。
2.  **为速度而近似**: 对于训练任务，“精确”模拟往往是多余的。投影到物理上合理的子空间（低权重 Pauli）可以在保留变分学习所需物理特性的同时，实现巨大的加速。
3.  **Scan 是必须的**: 对于 JAX 中的任何深层序列模型（RNN、深层线路、时间演化），为了避免编译时间爆炸，使用 `jax.lax.scan` 是强制性的。
